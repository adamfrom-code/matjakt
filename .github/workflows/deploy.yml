name: Deploy frontend to GitHub Pages

# Körs när CI (ci.yml) är GRÖN på main - inte på pushen. En röd svit
# deployar varken backend (Render-hooken i ci.yml) eller frontend (här).
on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  test:
    # Bara en GRÖN CI-körning för en PUSH till main får publicera; PR-körningar
    # (även från forkar vars gren heter main) släpps inte igenom.
    # workflow_dispatch är en medveten manuell väg som bara har npm test som
    # grind - inte e2e, hemlighetsskanning eller versionskontroll.
    if: github.event_name == 'workflow_dispatch' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push' && github.event.workflow_run.head_branch == 'main')
    runs-on: ubuntu-latest
    steps:
      # EXAKT den commit CI testade - inte main-HEAD när det här jobbet råkar
      # starta (en senare push kunde annars publiceras otestad).
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r backend/requirements.txt
      # npm test kör tests/build-native.test.js som behöver esbuild (devDependency).
      - run: npm ci
      - run: npm test

  deploy:
    needs: test
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Point the deployed site at the live backend
        run: sed -i 's#<meta name="matjakt-api-url" content="">#<meta name="matjakt-api-url" content="https://matjakt.onrender.com/api">#' frontend/app/index.html
      # Bundlad + minifierad kopia i dist/frontend (källorna orörda):
      # samma kod, en fil i stället för tretton moduler.
      - run: npm ci
      - run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist/frontend
      - uses: actions/deploy-pages@v4
        id: deployment
